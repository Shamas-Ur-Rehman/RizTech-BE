package config

// Collection names — single source of truth
const (
	ColUsers             = "users"
	ColRoles             = "roles"
	ColPermissions       = "permissions"
	ColProducts          = "products"
	ColOrders            = "orders"
	ColShippingSettings  = "shipping_settings"
)
